# Generated ML Dataset

This directory stores generated NumPy arrays used by the current dashboard, such as:

- `X_train.npy`
- `y_train.npy`

These files are reproducible artifacts and are ignored by Git. The dataset builder should be rerun when the source FITS file or preprocessing configuration changes.
