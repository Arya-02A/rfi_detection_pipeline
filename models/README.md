# Trained Models

This directory stores generated model artifacts such as:

```text
rfi_model.pkl
```

The model file is ignored by Git because it can be regenerated from the training pipeline. When publishing a release, a versioned model artifact may be attached separately together with its training configuration, dataset identity and evaluation metrics.
