# Dataset

Place the GMRT FITS observation used by the project here locally.

Do **not** commit large/raw observation files directly to GitHub. Add provenance, source/archive information, filename and access instructions here before publishing.

Expected application input:

```text
3C338_150.LTA_RRLL.RRLLFITS.fits
```

The current code expects a primary HDU table containing a `DATA` field compatible with the project's indexing logic.
